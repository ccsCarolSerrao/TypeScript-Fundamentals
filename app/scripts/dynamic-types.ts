module demo {
    var person: string;
    person = 'Carolina Serrão';

    console.log(person);
    console.log(person.substring(0,8));

    //person = {
    //    name: 'Camila',
    //    age: 28
    // }

    //console.log(person.substring(0,8));
    //console.log(person);
}